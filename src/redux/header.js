export const Header = (
  state = {
    collapse: true,
    guideHeight: "",
    navHeight: 0,
    sticky: false,
  },
  action
) => {
  switch (action.type) {
    default:
      return state;
  }
};
